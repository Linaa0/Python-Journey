[project]
name = "ai-project-one"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Linaa", email = "ishimwelina06@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "langchain>=1.3.14",
    "langchain-openai>=1.4.1",
    "langgraph>=1.2.10",
    "python-dotenv>=1.2.2",
]

[project.scripts]
ai-project-one = "ai_project_one:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
